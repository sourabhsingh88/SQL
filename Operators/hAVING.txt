1. SELECT MAX(SAL) , DEPTNO FROM EMP GROUP BY DEPTNO HAVING MAX(SAL) > 2700 ;

2. SELECT SUM(SAL) , JOB FROM EMP WHERE JOB != 'PRESIDENT' GROUP BY JOB HAVING SUM(SAL) > 3000 ;

3. SELECT COUNT(*) , DEPTNO FROM EMP GROUP BY DEPTNO HAVING COUNT(DEPTNO) > 6 ;

4. SELECT COUNT(JOB) , JOB FROM EMP GROUP BY JOB HAVING COUNT(JOB) = 0 ;

5. SELECT COUNT(SAL) , SAL FROM EMP GROUP BY SAL HAVING COUNT(SAL) > 1 ; 




1. SELECT * FROM EMP WHERE SAL > 2000 ORDER BY SAL ;

2.  SELECT COUNT(*) , JOB FROM EMP GROUP BY  JOB ORDER BY  JOB ;

3. SELECT MAX(SAL) , JOB FROM EMP GROUP BY JOB ORDER BY MAX(SAL) DESC ;

4. SELECT COUNT(*) ,DEPTNO FROM EMP GROUP BY DEPTNO HAVING COUNT(*)> 3 ORDER BY DEPTNO DESC ;

5. SELECT ENAME , DEPTNO , SAL , JOB , COMM , (SAL*12 )AS  ANNUAL_SALARY FROM EMP ORDER BY SAL*12 ;




ASSISMENT : 

1.  SELECT * FROM EMP WHERE empno = (select mgr from emp where ename = 'SMITH') ;

2.  SELECT * FROM EMP WHERE EMPNO = (SELECT MGR FROM EMP WHERE EMPNO = ( SELECT MGR FROM EMP WHERE ENAME ='SMITH')) ;

3.  SELECT * FROM EMP WHERE EMPNO = (SELECT MGR FROM EMP WHERE EMPNO =(SELECT MGR FROM EMP WHERE EMPNO = ( SELECT MGR FROM EMP WHERE ENAME ='SMITH'))) ;

4.  SELECT * FROM EMP WHERE EMPNO IN (SELECT MGR FROM EMP WHERE ENAME IN ('SMITH','TURNER') ) ;

5.  SELECT LOC FROM DEPT WHERE DEPTNO = (SELECT DEPTNO FROM EMP WHERE ENAME = 'SMITH' ) ;

6.  SELECT LOC FROM DEPT WHERE DEPTNO IN (SELECT DEPTNO FROM EMP WHERE ENAME IN( 'SMITH' ,'TURNER' )) ;

7.  SELECT * FROM EMP WHERE DEPTNO = (SELECT DEPTNO  FROM DEPT WHERE LOC = 'CHICAGO' ) ;

8.  SELECT * FROM EMP WHERE EXTRACT(MONTH FROM HIREDATE) IN ( SELECT EXTRACT(MONTH FROM HIREDATE) FROM EMP WHERE ENAME IN ('TURNER' ,'FORD')) ;

9.  SELECT * FROM EMP WHERE SAL IN (SELECT SAL FROM EMP GROUP BY SAL HAVING COUNT(*) > 1 ) ;

10. SELECT * FROM EMP WHERE SAL IN (SELECT SAL FROM EMP GROUP BY SAL HAVING COUNT(*) = 1 ) ;

11. SELECT MAX(SAL) FROM EMP ;

12. SELECT MAX(SAL) FROM EMP WHERE SAL < ( SELECT MAX(SAL) FROM EMP );

13. SELECT MAX(SAL) FROM EMP WHERE SAL < ( SELECT MAX(SAL) FROM EMP WHERE SAL < ( SELECT MAX(SAL) FROM EMP )  );

14. SELECT MIN(sAL) FROM EMP ;

15. SELECT MIN(SAL) FROM EMP WHERE SAL > (SELECT MIN(sAL) FROM EMP );

16. SELECTMIN(SAL) FROM EMP WHERE SAL > (SELECT MIN(SAL) FROM EMP WHERE SAL >(SELECT MIN(SAL) FROM EMP )) ;

17. SELECT MIN(HIREDATE) AS FIRST FROM EMP ;

18. SELECT MIN(HIREDATE) AS SECOND FROM EMP WHERE HIREDATE > (SELECT MIN(HIREDATE) FROM EMP );

19. SELECT MIN(HIREDATE) AS THIRD FROM EMP WHERE HIREDATE > (SELECT MIN(HIREDATE) FROM EMP WHERE HIREDATE > (SELECT MIN(HIREDATE) FROM EMP ) );

20. SELECT MAX(HIREDATE) AS LAST FROM EMP ;

21. SELECT MAX(HIREDATE) AS LASTsECOND FROM EMP WHERE HIREDATE <  ( SELECT MAX(HIREDATE) AS LAST FROM EMP );

22. SELECT * FROM EMP WHERE SAL = (SELECT MAX(SAL) FROM EMP) ;

23. SELECT * FROM EMP WHERE SAL > (SELECT SAL FROM EMP WHERE ENAME = 'TURNER') AND SAL < (SELECT SAL FROM EMP WHERE ENAME ='FORD') ;

24. SELECT * FROM EMP WHERE JOB IN (SELECT JOB FROM EMP WHERE ENAME IN ('SMITH' , 'TURNER')) ;

25. SELECT * FROM EMP WHERE HIREDATE IN (SELECT  MIN(HIREDATE) FROM EMP ) ;

26. SELECT * FROM EMP WHERE HIREDATE IN (SELECT  MAX(HIREDATE) FROM EMP ) ;

27. SELECT * FROM EMP WHERE SAL = ( SELECT MAX(sAL) FROM EMP WHERE SAL < (SELECT MAX(SAL) FROM EMP) ) ;




